Today i have learned about Shell Scripting topics:
echo, comment, read -p, command line arguments($0, $1, $3)

Created user using shell script
- created new user, assigned home path, shell, password, printed UID & GUID

